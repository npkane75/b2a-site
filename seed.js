const db = require('../models/index.js')
db.Episode.create(const Episodes = [
            {episodeId:"iDHCHgZETw0" }
        ]; 
        
        (err, createdEpisode)=> {
    if(err) return console.log(err);    
});

